# Count number of elements in array
arr = [1, 2, 3, 4, 5,6,7,8,9,0]
count = 0
# Loop through each element in the array
# '_' is used because we don't need the actual value
for _ in arr:
    count += 1
print(count)